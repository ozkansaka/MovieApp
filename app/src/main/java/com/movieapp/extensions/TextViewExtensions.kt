package com.movieapp.extensions

import android.widget.TextView
import com.movieapp.R
import java.time.LocalDate
import java.time.format.DateTimeFormatter

/*fun TextView.loadDate(date: String?) {
    val firstApiFormat = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss")
    val newDate = LocalDate.parse(date, firstApiFormat)
    val day = newDate.dayOfMonth
    val month = ("%02d".format(newDate.monthValue))
    val year = newDate.year
    val text = context.getString("Tarih") + ":" + "\n" + day + "." + month + "." + year
    this.text = text
}*/


